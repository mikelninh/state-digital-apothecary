const toast=message=>{const el=document.getElementById('toast');el.textContent=message;el.classList.add('show');clearTimeout(window.__toastTimer);window.__toastTimer=setTimeout(()=>el.classList.remove('show'),2600)};
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('.role').forEach(role=>role.addEventListener('click',()=>{document.querySelectorAll('.role').forEach(item=>item.classList.remove('active'));role.classList.add('active');document.getElementById('receiptRole').textContent=role.dataset.role;document.getElementById('receiptDeliverable').textContent=role.dataset.deliverable;document.getElementById('receiptValue').textContent=role.dataset.value}));
document.getElementById('applyRole').addEventListener('click',()=>{const selected=document.querySelector('.role.active');toast(`${selected.dataset.role} application preview opened. No contract has been created.`)});
const modal=document.getElementById('skillModal');document.getElementById('submitSkill').addEventListener('click',()=>modal.classList.add('open'));document.querySelectorAll('[data-close-modal]').forEach(button=>button.addEventListener('click',()=>modal.classList.remove('open')));modal.addEventListener('click',event=>{if(event.target===modal)modal.classList.remove('open')});document.getElementById('saveSkill').addEventListener('click',()=>{const value=document.getElementById('skill').value.trim();toast(value?`Prototype contributor profile saved: ${value}.`:'Add one concrete skill first.');if(value)modal.classList.remove('open')});
