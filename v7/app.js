const toast=message=>{const el=document.getElementById('toast');el.textContent=message;el.classList.add('show');clearTimeout(window.__toastTimer);window.__toastTimer=setTimeout(()=>el.classList.remove('show'),2800)};
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('[data-mission]').forEach(button=>button.addEventListener('click',()=>toast(`${button.dataset.mission}: scope, budget, proof and contracts would open before joining.`)));
document.getElementById('castVote').addEventListener('click',()=>toast('Good governance asks you to read the concerns before casting a vote.'));
const modal=document.getElementById('proposalModal');document.getElementById('openProposal').addEventListener('click',()=>modal.classList.add('open'));document.querySelectorAll('[data-close-modal]').forEach(button=>button.addEventListener('click',()=>modal.classList.remove('open')));modal.addEventListener('click',event=>{if(event.target===modal)modal.classList.remove('open')});document.getElementById('saveProposal').addEventListener('click',()=>{const value=document.getElementById('proposalProblem').value.trim();if(!value){toast('A proposal needs a concrete problem first.');return}localStorage.setItem('state-v7-proposal-draft',value);modal.classList.remove('open');toast('Proposal draft saved locally. No vote or public submission occurred.')});
